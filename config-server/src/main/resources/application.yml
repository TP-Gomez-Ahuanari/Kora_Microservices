# HTTP Server
server:
  port: 8888

spring:

  cloud:
    config:
      server:
        git:
          uri: https://github.com/TP-Gomez-Ahuanari/SpringCloudConfig-GitRepository.git

  profiles:
    active: dev